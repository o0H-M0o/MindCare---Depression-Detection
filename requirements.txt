# Streamlit Cloud installs dependencies from the repository root requirements.txt
#
# IMPORTANT:
# This app performs "immediate analysis" inside the Streamlit frontend
# (see frontend/pages/Journal.py and frontend/pages/My History.py), which imports
# backend models (Gemini + transformers/torch). Therefore the frontend deploy
# must include these runtime dependencies.

# Streamlit Framework
streamlit==1.51.0

# Data visualization
plotly==5.18.0
pandas

# API Communication
requests==2.31.0

# Database
supabase==2.0.3

# Utilities
python-dotenv==1.0.0

# Gemini (matches `import google.generativeai as genai`)
google-generativeai

# Sentiment analysis (matches backend/model/sentiment_model.py)
transformers>=4.35.0
torch>=2.1.0
scipy>=1.11.0
numpy>=1.24.0

# Export (matches frontend/utils/export_utils.py)
kaleido==0.2.1  # Must be 0.2.1 for Plotly 5.18.0 compatibility
reportlab==4.0.7
Pillow>=9.0.0
openpyxl>=3.1.0

# Optional dashboard features (wordcloud sections)
wordcloud
matplotlib
